plugins {
    id 'java'
}

allprojects {
    group = 'org.minerasoftware'
    version = '1.0-SNAPSHOT'

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(21)
        }
    }
}

subprojects {
    repositories {
        mavenCentral()
    }
}